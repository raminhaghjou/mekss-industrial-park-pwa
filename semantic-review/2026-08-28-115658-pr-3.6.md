# Transactionally scoped dashboard contract

Task 3.6 now derives dashboard scope from current manager relations in every final factory, request, gate-pass, and advertisement predicate. All dashboard reads and the park-manager capability gate execute in one RepeatableRead interactive transaction. The focused PostgreSQL contract owns its fixtures, covers all five authorized roles, and proves the eight-item cap and tie order with ten records. Empty, generated-scope, narrowed-scope, stale-user, real-database-failure, and safe-projection behavior remains covered. **Watch for:** no blocking or non-blocking issue was found in the requested scope.

**Result**: PASS — all three previous blockers are resolved.

**Blockers**: None.

**Non-blocking notes**: None.

**Verdict**: APPROVED

## High-level view

Park-manager scope is no longer represented by park ID arrays. Factory-derived reads use nested current-manager relations, advertisements use the relation directly, and capability removal is based on a park count from the same RepeatableRead snapshot.

The scoped e2e describe creates every actor and domain fixture it consumes. Its role matrix covers super administrators, park managers, factory owners, security guards, and government officials; ten equal-priority/equal-time requests establish both the hard cap and ID tie-break.

The preservation cases cover every generated subset of two managed parks, explicit narrowing, an unassigned manager, stale authentication state, and a physical PostgreSQL table failure. Narrow projections and negative assertions protect request, gate-pass, and advertisement data.

<details>
<summary>Issues (0)</summary>

No actionable issues.

</details>

<details><summary>Details</summary>

## Current manager relations define one authorization snapshot

`factoryScope` uses `park.managers.some(id)` and reaches the final factory count and every gate-pass, invoice, and request predicate through `factory.is` (`mekss-backend/src/core/management.service.ts:477-485`, `mekss-backend/src/core/management.service.ts:496-533`). Pending advertisements independently require the same manager relation in their final count and list predicates (`mekss-backend/src/core/management.service.ts:485-489`, `mekss-backend/src/core/management.service.ts:516-540`). No materialized park IDs participate in dashboard authorization.

The capability gate counts manager relations through the transaction client (`mekss-backend/src/core/management.service.ts:491-494`), and the interactive transaction encloses all dashboard reads at RepeatableRead (`mekss-backend/src/core/management.service.ts:473-608`). Records and mutation capabilities therefore share one authorization snapshot. Unit assertions pin the nested predicates and isolation level (`mekss-backend/src/core/management.service.spec.ts:122-137`, `mekss-backend/src/core/management.service.spec.ts:190-207`).

## Self-contained role and ordering evidence

The scoped describe creates its administrator and tenant actors inside its own `beforeAll` (`mekss-backend/test/management-foundation.e2e-spec.ts:182-207`); the supplied focused PostgreSQL 8/8 result corroborates independent execution. Super-admin and park-manager behavior is covered at `mekss-backend/test/management-foundation.e2e-spec.ts:313-342`, with factory-owner, security-guard, and government-official coverage at `mekss-backend/test/management-foundation.e2e-spec.ts:343-374`.

Ten pending `URGENT` requests share one timestamp and use ascending fixed IDs; the assertion requires exactly eight results in first-eight ID order (`mekss-backend/test/management-foundation.e2e-spec.ts:376-401`). Production applies scope and source ordering before `take: 8`, then ranks the merged feed by priority, timestamp, kind, and ID before slicing (`mekss-backend/src/core/management.service.ts:520-540`, `mekss-backend/src/core/management.service.ts:543-582`).

## Preservation, failure, and projection boundaries

All four subsets of the two park relations are checked for counts, IDs, and capability removal (`mekss-backend/test/management-foundation.e2e-spec.ts:403-450`). Post-commit narrowing, empty scope, and stale inactive/role-changed actors remain covered at `mekss-backend/test/management-foundation.e2e-spec.ts:451-494`.

The real failure case renames the PostgreSQL `Request` table, requires the stable 500 envelope without dashboard fields, and restores the table in `finally` (`mekss-backend/test/management-foundation.e2e-spec.ts:495-512`). Unit coverage separately requires read failures to propagate (`mekss-backend/src/core/management.service.spec.ts:231-248`).

Priority queries select only feed fields (`mekss-backend/src/core/management.service.ts:520-540`). Unit assertions reject injected sensitive fields (`mekss-backend/src/core/management.service.spec.ts:145-187`), and e2e assertions reject real contact, image, identity, and private-note data (`mekss-backend/test/management-foundation.e2e-spec.ts:284-295`, `mekss-backend/test/management-foundation.e2e-spec.ts:333-341`).

No new correctness, security, compatibility, or flakiness issue reached likely confidence. Supplied validation covers typecheck/lint, 31 unit tests, 6 preservation tests, 3 migration-contract tests, build, focused PostgreSQL dashboard 8/8, full PostgreSQL foundation 10/10, and owned-schema cleanup.

</details>

<details>
<summary>File map</summary>

- `mekss-backend/src/core/management.service.ts` — relational dashboard scope, RepeatableRead snapshot, bounded safe projections.
- `mekss-backend/src/core/management.service.spec.ts` — predicate, isolation, projection, empty-scope, and failure assertions.
- `mekss-backend/test/management-foundation.e2e-spec.ts` — self-owned fixtures and role, ordering, narrowing, stale-user, and database-failure contracts.

Full diff: `git diff main -- mekss-backend/src/core/management.service.ts mekss-backend/src/core/management.service.spec.ts mekss-backend/test/management-foundation.e2e-spec.ts`

</details>
