# Scoped, conflict-safe advertisement moderation verification

The current local diff strengthens Task 3.9 verification rather than changing the production moderation implementation: it expands the smoke path, repairs migration-check fixtures, stabilizes database-backed tests, and adjusts the moderation-board unit test. The active runtime remains the single `AppModule → CoreModule → ManagementController` path, with public approved reads, scoped admin reads, role-derived creation scope, conditional terminal transitions, and same-transaction auditing implemented there. The backend state machine and migration contracts are well covered, but the frontend still permits a moderation request when connectivity drops after a confirmation dialog has opened, and the changed test does not exercise that transition. **Watch for: [confirmed, MEDIUM] offline confirmation dialogs remain actionable and call approve/reject after an `offline` event.**

**Verdict**: NEEDS_CHANGES

## High-level view

Canonical active actors are reloaded by the database-backed guard, creation scope comes from active parks associated with the actor’s current role, and manager scope is applied before pagination. Legacy null-scope records remain super-admin-visible but cannot be moderated.

A conditional `PENDING` update and its audit insert share one transaction, giving sequential duplicates and concurrent approve/reject races one durable terminal result and one audit effect. Audit persistence failure rolls the transition back.

The UI confirms decisions, trims rejection reasons, blocks duplicate submissions while a mutation is pending, reconciles after `404`/`409`, and emits success only from the success path. Its connectivity guard applies only to card actions, so a dialog opened while online can still submit after the browser reports offline.

Static checks and focused builds/tests pass. Live PostgreSQL migration/e2e execution and the HTTP smoke could not run without an opted-in disposable database and running service.

<details>
<summary>Issues (1)</summary>

1. **Offline dialog mutation** — **[confirmed, MEDIUM]** Approval and rejection dialogs remain actionable after an online-to-offline transition and invoke the API. Disable or close open dialogs when offline, guard the mutation callbacks, and add approval/rejection tests proving no request or success notification occurs.

</details>

<details><summary>Details</summary>

## Offline transition after the confirmation dialog opens

**[confirmed, MEDIUM]** `AdvertisementModerationBoard.jsx:294-298` disables card-level approve/reject buttons with `decisionPending || !online`, but the open dialogs at lines 362-380 receive only `loading={decisionPending}` and call the mutations unconditionally. `ConfirmDialog.jsx:39-41,70-74` disables confirmation only for loading or a blank required reason. If connectivity drops after either dialog opens, “تایید و انتشار” or “ثبت رد آگهی” remains enabled and attempts the write, contrary to the offline non-mutation requirement.

The changed test fixes `navigator.onLine` to `true` at `AdvertisementModerationBoard.test.jsx:89` and covers cancellation plus a `409`, leaving this timing path unprotected. Pass connectivity into the dialog’s disabled state or close/disable open moderation dialogs when `online` becomes false, guard each mutation callback as a second line of defense, and add online-to-offline approval and rejection tests proving no API call and no success notification occur.

## CoreModule trust and transaction boundary

The route contract confirms that the older `src/advertisement/AdvertisementModule` is not registered. The active guard reloads role, approval, activity, and session version before authorization; the moderation transaction then enforces non-null authorized scope in its conditional `PENDING` update and writes moderator/time plus one audit through the same transaction client. The e2e contract exercises terminal states, invalid and Persian rejection reasons, unauthorized/out-of-scope actors, ambiguous scope, concurrent approve-versus-reject, fresh-session history, and forced audit rollback.

## Verification boundary

Backend and frontend builds, both type checks, both lint checks, 46 focused management/route tests, five advertisement UI/API tests, six preservation tests, three static moderation-migration tests, `git diff --check`, JavaScript syntax checking, and PowerShell parsing pass. The frontend test run emits only the existing React `act` deprecation warning.

The environment does not define both `MEKSS_TEST_DATABASE=1` and `DATABASE_URL`, so the isolated PostgreSQL integration runner, live migration checker, and database-backed e2e suites were not executed. The HTTP smoke also requires a running seeded service and `MEKSS_SMOKE_PASSWORD`; its behavior was inspected and its script parsed, not exercised against a server.

</details>

<details>
<summary>File map</summary>

- `mekss-backend/scripts/smoke-rbac-crud.ps1` — extends smoke coverage through scope, conflict, history, and public-read compatibility.
- `mekss-backend/test/advertisement-moderation.e2e-spec.ts` — adds fixture cleanup and sends only DTO-allowlisted contact fields.
- `mekss-backend/test/app.e2e-spec.ts` — updates health assertions to the active structured response.
- `mekss-backend/test/auth.e2e-spec.ts` — replaces stale auth assumptions with current registration, login, and OTP contracts.
- `mekss-backend/test/management-foundation.e2e-spec.ts` — bases global dashboard assertions on initial database state.
- `mekss-industrial-park/src/components/advertisements/AdvertisementModerationBoard.test.jsx` — fixes the deferred rejection type and relaxes a cancellation assertion.
- `scripts/check-moderation-migration.mjs` — corrects the moderator delete-action fixture identity.

Full diff: `git diff main`

</details>
